# -*- coding: utf-8 -*-

from gluon.scheduler import Scheduler
# recommended to start separate db for scheduler
sched_db = DAL('sqlite://sched_db.sqlite')
scheduler = Scheduler(sched_db, heartbeat=10)
